<header>
    <nav id="custom-nav">
        <div id="custom-logo-title">
            <a id="custom-logo-link" href="{{ path('hangout_index') }}">
                <img id="custom-logo" src="{{ asset('images/logoSortir.png') }}" alt="Logo de Sortir.com">
            </a>

            <div id="custom-title">
                <a href="{{ path('hangout_index') }}">Sortir.com</a>
            </div>
        </div>
        <div class="custom-menu" id="login_link">
            {% if app.user %}
                {% if is_granted('ROLE_ADMIN') %}
                    <p class="custom-nav-item"><a href="{{ path('hangout_index') }}">VILLE</a></p>
                    <p class="custom-nav-item"><a href="{{ path('hangout_index') }}">SITES</a></p>
                {% endif %}
                <p class="custom-nav-item"><a href="{{ path('hangout_index') }}">ACCEUIL</a></p>
                <p class="custom-nav-item"><a href="{{ path('app_profile') }}">MON PROFIL</a></p>
{#                <p class="custom-nav-item"><a href="{{ path('hangout_publish') }}">Créer une sortie</a></p>#}
                <p class="custom-nav-item"><a href="{{ path('app_logout') }}">SE DÉCONNECTER</a></p>
{#            {% else %}#}
{#                <p class="custom-nav-item"><a href="{{ path('app_register') }}">Register</a></p>#}
            {% endif %}
        </div>
    </nav>
</header>
