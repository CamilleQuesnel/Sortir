{% extends 'base.html.twig' %}

{% block body %}
    <h1>Panneau administrateur</h1>
    <div class="form-buttons">
        <a href="{{ path('admin_upload_csv') }}" class="btn-action">Télécharger une liste d'utilisateurs</a>
        <a href="{{ path('admin_create_user') }}" class="btn-action">Créer un utilisateur</a>
        <a href="{{ path('admin_list_users') }}" class="btn-action">Gérer les utilisateurs</a>
    </div>
{% endblock %}
