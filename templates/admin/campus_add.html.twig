{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ajouter un nouveau campus</h1>

    {{ form_start(form) }}
    {{ form_row(form.name) }}

    <button class="btn btn-primary">Ajouter le campus</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_campus') }}">← Retour à la liste des campus</a>
{% endblock %}
